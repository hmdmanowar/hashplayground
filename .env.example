OLLAMA_HOST=http://localhost:11434
JARVIS_MODEL=qwen2.5-coder
JARVIS_NAME=Jarvis
JARVIS_API_PORT=4000
JARVIS_MEMORY_DB=.jarvis/memory.sqlite
JARVIS_WORKSPACE_ROOT=.jarvis/workspace
JARVIS_AUDIT_LOG=.jarvis/audit.log
JARVIS_MAX_AGENT_STEPS=5
JARVIS_CONVERSATIONS_DB=.jarvis/conversations.sqlite
JARVIS_VISION_MODEL=llava
# Phase 6: repo the repo_* tools (git/build/test/file) operate on. Optional —
# defaults to wherever the process is started from (the repo root).
# JARVIS_REPO_ROOT=D:\React Workspace\Jarvis
